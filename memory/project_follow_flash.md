---
name: follow-flash-case-study
description: Follow Flash case study scaffold at app/work/follow-flash/page.tsx — no content yet, template structure only
metadata:
  type: project
---

# Follow Flash Case Study

**File:** `app/work/follow-flash/page.tsx`
**Route:** `/work/follow-flash`
**Status:** Scaffold only — no real content yet. All section copy is placeholder text.

## What it is
Social media management and automation tool for creators. Full case study content to be added later.

## Structure
Follows the canonical case study layout (same as Blocasset and Witan):
- Left sidebar with `TocNav` + `StickyBackLink` (desktop only)
- Diagonal-hatch decorative strip
- Content area with Overview metadata grid + right-half body sections

## TOC sections (all placeholder)
Overview → Backstory → Challenge (Problem statement) → Goal (Goal 1, Goal 2) → My Role → Research (heading 1, heading 2) → Design (section 1, section 2) → Impact → Feedback

## Impact cards
Four placeholder `{ stat: "—", label, desc }` cards — same KaliceTrial font style as Blocasset, no hover state.

## Feedback
One placeholder entry with orange-200→300 gradient avatar.

## AvatarCredits
Uses the generic `AvatarCredits` component — update if team members are known.

**How to apply:** When the user brings content for Follow Flash, fill in the placeholder text and TOC labels. Add cover image to `/public/` and replace the `bg-neutral-100 h-[400px]` placeholder div with a real `<Image>` or `<img>`.
